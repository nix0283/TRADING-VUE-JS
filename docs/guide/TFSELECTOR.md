# Timeframe Selector Components

Компоненты переключения таймфреймов для TradingVue.js в стиле TradingView.

## Установка

Скопируйте файлы в свой проект:
- `src/components/TFSelector.vue` — полный селектор
- `src/components/TFSelectorDropdown.vue` — компактный выпадающий селектор

## Поддерживаемые таймфреймы

### 📊 Полный список (как в TradingView)

| Категория | Таймфреймы | Описание |
|-----------|------------|----------|
| **Секунды** | `1s`, `5s`, `15s`, `30s` | Для скальпинга (скрыты по умолчанию) |
| **Минуты** | `1`, `2`, `3`, `5`, `10`, `15`, `20`, `30`, `45` | Внутридневная торговля |
| **Часы** | `1H`, `2H`, `3H`, `4H`, `6H`, `8H`, `12H` | Свинг-трейдинг |
| **Дни** | `D`, `2D`, `3D` | Позиционная торговля |
| **Недели** | `W`, `2W` | Среднесрочный анализ |
| **Месяцы** | `M`, `3M`, `6M`, `12M`, `Y` | Долгосрочный анализ |

### 🔥 Горячие клавиши

| Клавиша | Таймфрейм |
|---------|-----------|
| `1` | 1 минута |
| `3` | 3 минуты |
| `5` | 5 минут |
| `H` | 1 час |
| `4` | 4 часа |
| `D` | День |
| `W` | Неделя |
| `M` | Месяц |

## Использование

### Вариант 1: Полный селектор (TFSelector)

Отображает все таймфреймы в виде кнопок, сгруппированных по категориям.

```vue
<template>
  <trading-vue :data="chart" ref="tv"></trading-vue>
  <tf-selector
    v-model="timeframe"
    :night="true"
    :extended="true"
    @change="onTFChange"
  />
</template>

<script>
import TradingVue from 'trading-vue-js'
import TFSelector from './components/TFSelector.vue'

export default {
  components: { TradingVue, TFSelector },
  data() {
    return {
      timeframe: '1D',
      chart: { /* ... */ }
    }
  },
  methods: {
    onTFChange(tf) {
      // Загрузите данные для нового таймфрейма
      this.loadChartData(tf)
      this.$refs.tv.resetChart()
    }
  }
}
</script>
```

### Вариант 2: Выпадающий селектор (TFSelectorDropdown)

Компактный вариант с выпадающим меню. Подходит для мобильных устройств.

```vue
<template>
  <trading-vue :data="chart" ref="tv"></trading-vue>
  <tf-selector-dropdown
    v-model="timeframe"
    :night="true"
    @change="onTFChange"
  />
</template>

<script>
import TFSelectorDropdown from './components/TFSelectorDropdown.vue'

export default {
  components: { TFSelectorDropdown }
}
</script>
```

## Props

### TFSelector

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `value` | String | `'1D'` | Выбранный таймфрейм (v-model) |
| `night` | Boolean | `true` | Тёмная тема |
| `showSeconds` | Boolean | `false` | Показать секундные таймфреймы |
| `extended` | Boolean | `false` | Показать расширенный набор |

### TFSelectorDropdown

| Prop | Тип | По умолчанию | Описание |
|------|-----|--------------|----------|
| `value` | String | `'1D'` | Выбранный таймфрейм (v-model) |
| `night` | Boolean | `true` | Тёмная тема |

## Events

| Event | Параметр | Описание |
|-------|----------|----------|
| `input` | `tf: string` | Изменение v-model |
| `change` | `tf: string` | Выбор нового таймфрейма |

## Формат таймфреймов

Компоненты используют стандартный формат TradingView:

```
<number><unit>

где:
- number: целое число
- unit: s (секунды), m (минуты), h (часы), D (дни), W (недели), M (месяцы)
```

Примеры:
- `1` или `1m` — 1 минута
- `15` или `15m` — 15 минут
- `60` или `1H` — 1 час
- `240` или `4H` — 4 часа
- `1D` — 1 день
- `1W` — 1 неделя

## Кастомизация

### Изменение цветов

```css
/* Переопределите CSS переменные */
.tvjs-tf-selector {
  --tf-bg: #1e2224;
  --tf-active: #2962ff;
  --tf-text: #d1d4dc;
}
```

### Добавление своих таймфреймов

```vue
<tf-selector v-model="tf">
  <template #custom>
    <button @click="selectCustom('2h30')">2h30</button>
  </template>
</tf-selector>
```

## Демо

Запустите тестовый сервер:

```bash
cd trading-vue-js
npm install
npm run test
```

Выберите тест "TimeframesDemo" для просмотра компонентов.
